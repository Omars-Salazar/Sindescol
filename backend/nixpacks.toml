# Nixpacks Configuration for SINDESCOL Backend
# Forces Railway to use Nixpacks instead of Railpack

[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = []

[start]
cmd = 'npm start'
